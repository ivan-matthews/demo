++ Класс БД, Модель

++ Класс Сессий, Куков

++ Крон
+++ CLI cron
--- Класс Крон !deprecated! - крон из запускаем из cli
+++ Задачи Крон
+++ Файл Крон

++ Класс Контроллеров

++ Класс Респонс

++ Консоль:
+++ миграции: таблицы и альтер
+++ миграция, дроп, мейк
+++ миграции: инзерты демо-данных
+++ псевдонимы комманд
+++ контроллер
+++ БД-интерфейсы (в смысле глобальном, а не рнр)

++ Обработка ошибок
+++ если дебаг выкл 0:
	 записываем ошибки хтмл
	 показываем ошибки кли
++++ шатдавн:
		показываем ошибку 500 в хтмл
		записываем ошибки хтмл
		показываем ошибки кли
+++ если дебаг вкл 1:
	 показываем ошибки хтмл
	 показываем ошибки кли
++++ шатдавн:
		показываем ошибки хтмл
		показываем ошибки кли

++ все остальное:
+++ каш
+++ формы
--- поля !deprecated!
+++ валидаторы

++ Класс Виджетов	(лонг-бокс)
-- Класс Плагинов	!deprecated! - есть хуки
-- СМС				(лонг-бокс)
++ Хуки

++ Почта
++ Нотисы
++ Jevix
++ Въйу

++ Ланг		!сначала нужна авторизация!
++ Юзер		!сначала нужна авторизация!
++ Аццесс	!сначала нужна авторизация!

++ Файлы
+++ мультипл + изменить "name -> name[]"
+++ аццепт
+++ для массива файлов - вызвать мультипл, с него обход [препаренного] массива
+++ для одного: сингл

++	$user->validateAuthorize();			 - часть контроллера авторизации, можно вызвать плагином
++	$user->refreshAuthCookieTime();		 - часть контроллера авторизации, можно вызвать плагином
++	$user->resetCSRFToken();			 - часть контроллера авторизации, можно вызвать плагином


----------------------------------------------------------------------------------------------------------

- Контроллер:
++ Редирект
-- Контент (+ ссылки с виджета в футере преобразовать ид итемы; в регистрации)
++ Аватар
++ Статус
++ Журнал
++ Поиск
++ Почта (ну там такое осталось: архив, корзина, добавить контакт)
++ Сайдбар для мобильной версии
-- Фильтр на хук					(!deprecated!)



----------------------------------------------------------------------------------------------------------

- Блог
-- переписать ссылки в футере:
	faq 		- отдельный контроллер
	feedback 	- отдельный контроллер
	sitemap 	- отдельный контроллер
	support 	- отдельный контроллер
	site-news 	- отдельный контроллер (контент, типа блог)

-- Категории								(лонг-бокс)
--- Таблица категорий
--- Виджет в сайдбар (страница блогов)
--- Все категории
--- Все посты по категории
--- Добавление
--- Удаление
--- Редактирование категории

++ Комментарии
+++ Список камментов в виджете
+++ в этом же виджете отдельным массивом параметры рендинга формы добавления каммента
+++ входящие параметры:
		контроллер;
		экшон;
		ID записи;
			++++ по ID записи выбираем получателя	 ??? - ставим из контроллера, где вызываем виджет камментов
			++++ отправитель - из сессии			 ???

Параметры для виджета ставятся из массива `allowed_controllers` файла core/controllers/comments/config/params.php
	то есть для каждого контроллера прийдется прописывать отдельные параметры (спс префиксам для полей!)


-- Фидбек
--- Форма отправки
--- Шаблон майла
--- В конфиге контактные номера; э-мейлы; адреса