++ Класс БД, Модель

++ Класс Сессий, Куков

++ Крон
+++ CLI cron
--- Класс Крон !deprecated!
+++ Задачи Крон
+++ Файл Крон

++ Класс Контроллеров

-- Класс Виджетов

++ Класс Респонс

++ Консоль:
+++ миграции: таблицы и альтер
+++ миграция, дроп, мейк
+++ миграции: инзерты демо-данных
+++ псевдонимы комманд
+++ контроллер
+++ БД-интерфейсы (в смыле глобальном, а не рнр)

++ Обработка ошибок
+++ если дебаг выкл 0:
	 записываем ошибки хтмл
	 показываем ошибки кли
++++ шатдавн:
		показываем ошибку 500 в хтмл
		записываем ошибки хтмл
		показываем ошибки кли
+++ если дебаг вкл 1:
	 показываем ошибки хтмл
	 показываем ошибки кли
++++ шатдавн:
		показываем ошибки хтмл
		показываем ошибки кли

+- все остальное:
++- каш
--- формы
--- поля
--- валидаторы

-- Ланг		!сначала нужна авторизация
-- Юзер		!сначала нужна авторизация
++ Аццесс	!сначала нужна авторизация




















